//these are all the firebase exports that we use in verification centralized into one location
import * as auth from "./auth";
import * as firebase from "./firebase";
import * as db from "./db";

export { auth, db, firebase };
